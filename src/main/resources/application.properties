spring.application.name=new-bet
# MySQL Database Configuration
# Replace 'betting_db' with your database name. It will be created if it doesn't exist.
spring.datasource.url=jdbc:mysql://localhost:3306/new_bet?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=HEROsprint@123

# JPA/Hibernate Configuration
# 'update' will create/update the table based on your Entity class.
spring.jpa.hibernate.ddl-auto=update
# 'true' will show the SQL queries in the console, which is useful for debugging.
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

server.port=8081
